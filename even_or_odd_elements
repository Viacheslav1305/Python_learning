# Вывод каждого четного элемента строки, если четное количество элементов в строке и наоборот

def even_or_odd_element(a):
    b = str(a)
    if len(b) % 2 == 0:
        return b[1::2]
    else:
        return b[::2]
