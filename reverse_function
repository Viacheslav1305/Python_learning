# Создать функцию, которая разворачивает строку, если там четное количество элементов

def reverse_string(s):
    a = str(s)
    if len(a) % 2 == 0:
        return a[::-1]
    else:
        print('Нельзя развернуть строку')
